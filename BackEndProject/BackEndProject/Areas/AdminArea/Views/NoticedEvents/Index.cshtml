@model List<NoticedEvent>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/AdminArea/Views/Shared/_AdminLayout.cshtml";
    int count = 1;
}

<div class="container">
    <div class="row">
        <div class="header d-flex justify-content-between mt-3">
            <h1>Categories</h1>
            <div>

                <a class="btn btn-success" asp-action="create">Create Category</a>
            </div>
        </div>
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>
                            Number
                        </th>
                        <th>
                            Date
                        </th>
                        <th>
                            Event Detail
                        </th>
                        <th>
                            Settings
                        </th>

                    </tr>
                </thead>
                <tbody>
                    @foreach (var noticedEvent in Model)
                    {
                    <tr>
                        <td>
                            @count
                        </td>
                        <td>
                            @noticedEvent.Date
                        </td>
                        <td>
                            @noticedEvent.EvenetDetail.Substring(0, 10) ...;
                        </td>
                        <td>
                            <a class="btn btn-primary btn-sm" asp-action="Detail" asp-route-id="@noticedEvent.Id">
                                <i class="mdi mdi-account-box-outline"></i>
                            </a>
                            <a class="btn btn-warning btn-sm" asp-action="Edit" asp-route-id="@noticedEvent.Id">
                                <i class="mdi mdi-credit-card"></i>
                            </a>
                            <form method="post" asp-action="Delete" asp-route-id="@noticedEvent.Id" style="display:inline-block">
                                <button class="btn btn-danger btn-sm" type="submit">
                                    <i class="mdi mdi-delete-forever"> </i>
                                </button>
                            </form>
                        </td>
                    </tr>
                        count++;
                    }

                </tbody>
            </table>
        </div>
    </div>
</div>





